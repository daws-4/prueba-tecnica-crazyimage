{
  "name": "andina-cargo",
  "version": "0.1.0",
  "private": true,
  "description": "Panel unico de seguimiento de envios para Andina Cargo",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:contracts": "npm run build -w @andina/contracts",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
